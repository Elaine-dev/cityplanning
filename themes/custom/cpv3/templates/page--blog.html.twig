    <!-- Fixed navbar -->
    {% include directory ~ '/partials/header.html.twig' %}

	{# page-banner #}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
				{{ page.top_banner }}
            </div>
        </div>
    </div>{# End page-banner #}

    <!-- contain -->
    <div class="container" id="main-container">
        {# Main content #}
        <div class="row">
        	<div class="col-md-12">{{ page.title_bar }}</div>
        </div>
        <div class="row paddingtop15 padding-bottom60">
            {# Left-side bar #}
            <div class="col-md-9">
                <div class="row padding-left0">
        <div class="col-md-12">
          <p>
            City Planning Department staff engages with community members on a variety of land use matters. In addition to hosting events&mdash;or speaking at neighborhood councils, they blog periodically to share updates and insights on planning matters. Click
            below to access past entries and see firsthand how the Department is fostering greater engagement online through the use of digital technology. For additional information, contact Rayna Plummer by email <a href="mailto:rayna.plummer@lacity.org">rayna.plummer@lacity.org</a> or phone (213) 978-1234.
          </p>
          {# content goes here #}
          {{ page.block_eir }}
          {{ page.content }}
        </div>
                </div>
            </div>{# End Left-side bar #}

            {# Right-side bar #}
            <div class="col-md-3 sidebar-menu hidden-sm hidden-xs">
                <div class="top-bar"></div>
                  {{ page.side_menu }}
            </div>{# End Right-side bar #}
        </div>
    </div>{# End container #}

    {# stay connected #}
	{% include directory ~ '/partials/stay_connected.html.twig' %}
	{# // End #say-connected #}

    <!-- footer section -->
    {% include directory ~ '/partials/footer.html.twig' %}
