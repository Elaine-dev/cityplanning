{% set class = ['padding-left-0','','','padding-right-0'] %}
{% for item in items %}
    <div class="col-md-3 white--bg {{ class[(loop.index0)] }}" {{ item.attributes }}>{{ item.content }}</div>
{% endfor %}