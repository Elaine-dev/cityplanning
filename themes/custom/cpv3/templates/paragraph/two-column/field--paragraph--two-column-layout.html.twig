{% set class = ['padding-left0', 'padding-right0'] %}
{% for item in items %}
    <div class="col-md-6 white--bg {{ class[(loop.index0)] }}" {{ item.attributes }}>{{ item.content }}</div>
{% endfor %}